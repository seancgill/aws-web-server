module.exports = {
  handleEnergyRequest: (req, res, io) => {
    res.status(200).send('Energy request placeholder');
  },
  handleStatusRequest: (req, res, io) => {
    res.status(200).send('Status request placeholder');
  }
};
